export interface destino{
    id: number
    nombre: string
    ruta: string
    ip: string
    puerto: string
}

export interface destinoPost{
    nombre: string
    ruta: string
    ip: string
    puerto: string
}